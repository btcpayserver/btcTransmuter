@model ExtensionHeadersViewComponentViewModel

@foreach (var ext in Model.Extensions)
{
    if (!string.IsNullOrEmpty(ext.HeaderPartial))
    {
        @await Html.PartialAsync(ext.HeaderPartial); 
    }
}