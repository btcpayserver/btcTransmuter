@model RemoveExternalServiceViewModel


@{
    ViewData["Title"] = "Remove External Service";
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>External Service</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ExternalService.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ExternalService.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ExternalService.Type)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ExternalService.Type)
        </dd>
    </dl>
    
    <form asp-action="RemoveExternalServicePost" asp-route-id="@Model.ExternalService.Id"  method="post" class="mt-2">
        <input type="submit" value="Delete" class="btn btn-primary" /> 
        <a asp-action="GetServices" class="btn btn-secondary">Back to List</a>
    </form>
</div>
